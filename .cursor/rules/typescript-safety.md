# TypeScript Safety

- Do not use `any` unless justified.
- Validate runtime boundaries with Zod.
- Fail fast on invalid environment variables.
- Require typecheck before merge.
- Isolate vendor-specific adapters.
